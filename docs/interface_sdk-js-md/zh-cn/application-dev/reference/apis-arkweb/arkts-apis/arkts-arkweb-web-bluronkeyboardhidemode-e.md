# BlurOnKeyboardHideMode

Enum type supplied to [blurOnKeyboardHideMode](arkts-arkweb-web-webattribute-i.md#bluronkeyboardhidemode) for setting the web blurOnKeyboardHide mode.@enum { number }

**起始版本：** 23

**ArkTS模式：** 仅支持ArkTS-Sta，ArkTS-Sta起始版本为23。

**系统能力：** SystemCapability.Web.Webview.Core

## SILENT

```TypeScript
SILENT = 0
```

The focused input elements on webview will not blur when soft keyboard is hidden manually.

**起始版本：** 23

**ArkTS模式：** 仅支持ArkTS-Sta，ArkTS-Sta起始版本为23。

**系统能力：** SystemCapability.Web.Webview.Core

## BLUR

```TypeScript
BLUR = 1
```

The focused input elements on webview will blur when soft keyboard is hidden manually.

**起始版本：** 23

**ArkTS模式：** 仅支持ArkTS-Sta，ArkTS-Sta起始版本为23。

**系统能力：** SystemCapability.Web.Webview.Core
