# toSorted

## 导入模块

```TypeScript
```

## toSorted

```TypeScript
export function toSorted(self: FixedArray<boolean>): FixedArray<boolean>
```

返回元素按升序排序后的新数组。

**起始版本：** 26.0.0

**ArkTS模式：** 仅支持ArkTS-Sta，ArkTS-Sta起始版本为26.0.0。

**模型约束：** 此接口仅可在Stage模型下使用。

**系统能力：** SystemCapability.Utils.Lang

**参数：**

| 参数名 | 类型 | 必填 |
| --- | --- | --- |
| self | FixedArray & lt;boolean & gt; | 是 |

**返回值：**

| 类型 |
| --- |
| FixedArray & lt;boolean & gt; |


## toSorted

```TypeScript
export function toSorted(self: FixedArray<boolean>, comparator: (a: boolean, b: boolean) => int): FixedArray<boolean>
```

返回使用指定比较函数排序后的新数组。

**起始版本：** 26.0.0

**ArkTS模式：** 仅支持ArkTS-Sta，ArkTS-Sta起始版本为26.0.0。

**模型约束：** 此接口仅可在Stage模型下使用。

**系统能力：** SystemCapability.Utils.Lang

**参数：**

| 参数名 | 类型 | 必填 |
| --- | --- | --- |
| self | FixedArray & lt;boolean & gt; | 是 |
| comparator | (a: boolean, b: boolean) = & gt; int | 是 |

**返回值：**

| 类型 |
| --- |
| FixedArray & lt;boolean & gt; |


## toSorted

```TypeScript
export function toSorted(self: FixedArray<byte>): FixedArray<byte>
```

返回元素按升序排序后的新数组。

**起始版本：** 26.0.0

**ArkTS模式：** 仅支持ArkTS-Sta，ArkTS-Sta起始版本为26.0.0。

**模型约束：** 此接口仅可在Stage模型下使用。

**系统能力：** SystemCapability.Utils.Lang

**参数：**

| 参数名 | 类型 | 必填 |
| --- | --- | --- |
| self | FixedArray & lt;byte & gt; | 是 |

**返回值：**

| 类型 |
| --- |
| FixedArray & lt;byte & gt; |


## toSorted

```TypeScript
export function toSorted(self: FixedArray<byte>, comparator: (a: byte, b: byte) => int): FixedArray<byte>
```

返回使用指定比较函数排序后的新数组。

**起始版本：** 26.0.0

**ArkTS模式：** 仅支持ArkTS-Sta，ArkTS-Sta起始版本为26.0.0。

**模型约束：** 此接口仅可在Stage模型下使用。

**系统能力：** SystemCapability.Utils.Lang

**参数：**

| 参数名 | 类型 | 必填 |
| --- | --- | --- |
| self | FixedArray & lt;byte & gt; | 是 |
| comparator | (a: byte, b: byte) = & gt; int | 是 |

**返回值：**

| 类型 |
| --- |
| FixedArray & lt;byte & gt; |


## toSorted

```TypeScript
export function toSorted(self: FixedArray<short>): FixedArray<short>
```

返回元素按升序排序后的新数组。

**起始版本：** 26.0.0

**ArkTS模式：** 仅支持ArkTS-Sta，ArkTS-Sta起始版本为26.0.0。

**模型约束：** 此接口仅可在Stage模型下使用。

**系统能力：** SystemCapability.Utils.Lang

**参数：**

| 参数名 | 类型 | 必填 |
| --- | --- | --- |
| self | FixedArray & lt;short & gt; | 是 |

**返回值：**

| 类型 |
| --- |
| FixedArray & lt;short & gt; |


## toSorted

```TypeScript
export function toSorted(self: FixedArray<short>, comparator: (a: short, b: short) => int): FixedArray<short>
```

返回使用指定比较函数排序后的新数组。

**起始版本：** 26.0.0

**ArkTS模式：** 仅支持ArkTS-Sta，ArkTS-Sta起始版本为26.0.0。

**模型约束：** 此接口仅可在Stage模型下使用。

**系统能力：** SystemCapability.Utils.Lang

**参数：**

| 参数名 | 类型 | 必填 |
| --- | --- | --- |
| self | FixedArray & lt;short & gt; | 是 |
| comparator | (a: short, b: short) = & gt; int | 是 |

**返回值：**

| 类型 |
| --- |
| FixedArray & lt;short & gt; |


## toSorted

```TypeScript
export function toSorted(self: FixedArray<int>): FixedArray<int>
```

返回元素按升序排序后的新数组。

**起始版本：** 26.0.0

**ArkTS模式：** 仅支持ArkTS-Sta，ArkTS-Sta起始版本为26.0.0。

**模型约束：** 此接口仅可在Stage模型下使用。

**系统能力：** SystemCapability.Utils.Lang

**参数：**

| 参数名 | 类型 | 必填 |
| --- | --- | --- |
| self | FixedArray & lt;int & gt; | 是 |

**返回值：**

| 类型 |
| --- |
| FixedArray & lt;int & gt; |


## toSorted

```TypeScript
export function toSorted(self: FixedArray<int>, comparator: (a: int, b: int) => int): FixedArray<int>
```

返回使用指定比较函数排序后的新数组。

**起始版本：** 26.0.0

**ArkTS模式：** 仅支持ArkTS-Sta，ArkTS-Sta起始版本为26.0.0。

**模型约束：** 此接口仅可在Stage模型下使用。

**系统能力：** SystemCapability.Utils.Lang

**参数：**

| 参数名 | 类型 | 必填 |
| --- | --- | --- |
| self | FixedArray & lt;int & gt; | 是 |
| comparator | (a: int, b: int) = & gt; int | 是 |

**返回值：**

| 类型 |
| --- |
| FixedArray & lt;int & gt; |


## toSorted

```TypeScript
export function toSorted(self: FixedArray<long>): FixedArray<long>
```

返回元素按升序排序后的新数组。

**起始版本：** 26.0.0

**ArkTS模式：** 仅支持ArkTS-Sta，ArkTS-Sta起始版本为26.0.0。

**模型约束：** 此接口仅可在Stage模型下使用。

**系统能力：** SystemCapability.Utils.Lang

**参数：**

| 参数名 | 类型 | 必填 |
| --- | --- | --- |
| self | FixedArray & lt;long & gt; | 是 |

**返回值：**

| 类型 |
| --- |
| FixedArray & lt;long & gt; |


## toSorted

```TypeScript
export function toSorted(self: FixedArray<long>, comparator: (a: long, b: long) => int): FixedArray<long>
```

返回使用指定比较函数排序后的新数组。

**起始版本：** 26.0.0

**ArkTS模式：** 仅支持ArkTS-Sta，ArkTS-Sta起始版本为26.0.0。

**模型约束：** 此接口仅可在Stage模型下使用。

**系统能力：** SystemCapability.Utils.Lang

**参数：**

| 参数名 | 类型 | 必填 |
| --- | --- | --- |
| self | FixedArray & lt;long & gt; | 是 |
| comparator | (a: long, b: long) = & gt; int | 是 |

**返回值：**

| 类型 |
| --- |
| FixedArray & lt;long & gt; |


## toSorted

```TypeScript
export function toSorted(self: FixedArray<float>): FixedArray<float>
```

返回元素按升序排序后的新数组。

**起始版本：** 26.0.0

**ArkTS模式：** 仅支持ArkTS-Sta，ArkTS-Sta起始版本为26.0.0。

**模型约束：** 此接口仅可在Stage模型下使用。

**系统能力：** SystemCapability.Utils.Lang

**参数：**

| 参数名 | 类型 | 必填 |
| --- | --- | --- |
| self | FixedArray & lt;float & gt; | 是 |

**返回值：**

| 类型 |
| --- |
| FixedArray & lt;float & gt; |


## toSorted

```TypeScript
export function toSorted(self: FixedArray<float>, comparator: (a: float, b: float) => int): FixedArray<float>
```

返回使用指定比较函数排序后的新数组。

**起始版本：** 26.0.0

**ArkTS模式：** 仅支持ArkTS-Sta，ArkTS-Sta起始版本为26.0.0。

**模型约束：** 此接口仅可在Stage模型下使用。

**系统能力：** SystemCapability.Utils.Lang

**参数：**

| 参数名 | 类型 | 必填 |
| --- | --- | --- |
| self | FixedArray & lt;float & gt; | 是 |
| comparator | (a: float, b: float) = & gt; int | 是 |

**返回值：**

| 类型 |
| --- |
| FixedArray & lt;float & gt; |


## toSorted

```TypeScript
export function toSorted(self: FixedArray<double>): FixedArray<double>
```

返回元素按升序排序后的新数组。

**起始版本：** 26.0.0

**ArkTS模式：** 仅支持ArkTS-Sta，ArkTS-Sta起始版本为26.0.0。

**模型约束：** 此接口仅可在Stage模型下使用。

**系统能力：** SystemCapability.Utils.Lang

**参数：**

| 参数名 | 类型 | 必填 |
| --- | --- | --- |
| self | FixedArray & lt;double & gt; | 是 |

**返回值：**

| 类型 |
| --- |
| FixedArray & lt;double & gt; |


## toSorted

```TypeScript
export function toSorted(self: FixedArray<double>, comparator: (a: double, b: double) => int): FixedArray<double>
```

返回使用指定比较函数排序后的新数组。

**起始版本：** 26.0.0

**ArkTS模式：** 仅支持ArkTS-Sta，ArkTS-Sta起始版本为26.0.0。

**模型约束：** 此接口仅可在Stage模型下使用。

**系统能力：** SystemCapability.Utils.Lang

**参数：**

| 参数名 | 类型 | 必填 |
| --- | --- | --- |
| self | FixedArray & lt;double & gt; | 是 |
| comparator | (a: double, b: double) = & gt; int | 是 |

**返回值：**

| 类型 |
| --- |
| FixedArray & lt;double & gt; |


## toSorted

```TypeScript
export function toSorted(self: FixedArray<char>): FixedArray<char>
```

返回元素按升序排序后的新数组。

**起始版本：** 26.0.0

**ArkTS模式：** 仅支持ArkTS-Sta，ArkTS-Sta起始版本为26.0.0。

**模型约束：** 此接口仅可在Stage模型下使用。

**系统能力：** SystemCapability.Utils.Lang

**参数：**

| 参数名 | 类型 | 必填 |
| --- | --- | --- |
| self | FixedArray & lt;char & gt; | 是 |

**返回值：**

| 类型 |
| --- |
| FixedArray & lt;char & gt; |


## toSorted

```TypeScript
export function toSorted(self: FixedArray<char>, comparator: (a: char, b: char) => int): FixedArray<char>
```

返回使用指定比较函数排序后的新数组。

**起始版本：** 26.0.0

**ArkTS模式：** 仅支持ArkTS-Sta，ArkTS-Sta起始版本为26.0.0。

**模型约束：** 此接口仅可在Stage模型下使用。

**系统能力：** SystemCapability.Utils.Lang

**参数：**

| 参数名 | 类型 | 必填 |
| --- | --- | --- |
| self | FixedArray & lt;char & gt; | 是 |
| comparator | (a: char, b: char) = & gt; int | 是 |

**返回值：**

| 类型 |
| --- |
| FixedArray & lt;char & gt; |
