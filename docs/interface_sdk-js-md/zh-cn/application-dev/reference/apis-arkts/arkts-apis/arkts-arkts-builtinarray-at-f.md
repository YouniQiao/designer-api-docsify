# at

## 导入模块

```TypeScript
```

## at

```TypeScript
export function at(self: FixedArray<boolean>, index: int): Boolean
```

接受一个整数值并返回该索引处的元素，允许传入 正整数和负整数。负整数表示从数组最后一个元素 开始倒数。

**起始版本：** 26.0.0

**ArkTS模式：** ArkTS-Sta起始版本为26.0.0。

**模型约束：** 此接口仅可在Stage模型下使用。

<!--Device-unnamed-export function at(self: FixedArray<boolean>, index: int): Boolean--><!--Device-unnamed-export function at(self: FixedArray<boolean>, index: int): Boolean-End-->

**系统能力：** SystemCapability.Utils.Lang

**参数：**

| 参数名 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| self | FixedArray&lt;boolean&gt; | 是 | 执行`at`操作的数组。 |
| index | int | 是 | 待返回数组元素的索引，从0开始计数。 <br>取值约束：应为整数。 |

**返回值：**

| 类型 | 说明 |
| --- | --- |
| Boolean | 指定索引处的元素。 |


## at

```TypeScript
export function at(self: FixedArray<byte>, index: int): Byte
```

接受一个整数值并返回该索引处的元素，允许传入正整数和负整数。 负整数表示从数组最后一个元素开始倒数。

**起始版本：** 26.0.0

**ArkTS模式：** ArkTS-Sta起始版本为26.0.0。

**模型约束：** 此接口仅可在Stage模型下使用。

<!--Device-unnamed-export function at(self: FixedArray<byte>, index: int): Byte--><!--Device-unnamed-export function at(self: FixedArray<byte>, index: int): Byte-End-->

**系统能力：** SystemCapability.Utils.Lang

**参数：**

| 参数名 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| self | FixedArray&lt;byte&gt; | 是 | 执行`at`操作的数组。 |
| index | int | 是 | 待返回数组元素的索引，从0开始计数。 <br>取值约束：应为整数。 |

**返回值：**

| 类型 | 说明 |
| --- | --- |
| Byte | 指定索引处的元素。 |


## at

```TypeScript
export function at(self: FixedArray<short>, index: int): Short
```

接受一个整数值并返回该索引处的元素，允许传入正整数和负整数。 负整数表示从数组最后一个元素开始倒数。

**起始版本：** 26.0.0

**ArkTS模式：** ArkTS-Sta起始版本为26.0.0。

**模型约束：** 此接口仅可在Stage模型下使用。

<!--Device-unnamed-export function at(self: FixedArray<short>, index: int): Short--><!--Device-unnamed-export function at(self: FixedArray<short>, index: int): Short-End-->

**系统能力：** SystemCapability.Utils.Lang

**参数：**

| 参数名 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| self | FixedArray&lt;short&gt; | 是 | 执行`at`操作的数组。 |
| index | int | 是 | 待返回数组元素的索引，从0开始计数。 <br>取值约束：应为整数。 |

**返回值：**

| 类型 | 说明 |
| --- | --- |
| Short | 指定索引处的元素。 |


## at

```TypeScript
export function at(self: FixedArray<int>, index: int): Int
```

接受一个整数值并返回该索引处的元素，允许传入正整数和负整数。 负整数表示从数组最后一个元素开始倒数。

**起始版本：** 26.0.0

**ArkTS模式：** ArkTS-Sta起始版本为26.0.0。

**模型约束：** 此接口仅可在Stage模型下使用。

<!--Device-unnamed-export function at(self: FixedArray<int>, index: int): Int--><!--Device-unnamed-export function at(self: FixedArray<int>, index: int): Int-End-->

**系统能力：** SystemCapability.Utils.Lang

**参数：**

| 参数名 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| self | FixedArray&lt;int&gt; | 是 | 执行`at`操作的数组。 |
| index | int | 是 | 待返回数组元素的索引，从0开始计数。 <br>取值约束：应为整数。 |

**返回值：**

| 类型 | 说明 |
| --- | --- |
| Int | 指定索引处的元素。 |


## at

```TypeScript
export function at(self: FixedArray<long>, index: int): Long
```

接受一个整数值并返回该索引处的元素，允许传入正整数和负整数。 负整数表示从数组最后一个元素开始倒数。

**起始版本：** 26.0.0

**ArkTS模式：** ArkTS-Sta起始版本为26.0.0。

**模型约束：** 此接口仅可在Stage模型下使用。

<!--Device-unnamed-export function at(self: FixedArray<long>, index: int): Long--><!--Device-unnamed-export function at(self: FixedArray<long>, index: int): Long-End-->

**系统能力：** SystemCapability.Utils.Lang

**参数：**

| 参数名 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| self | FixedArray&lt;long&gt; | 是 | 执行`at`操作的数组。 |
| index | int | 是 | 待返回数组元素的索引，从0开始计数。 <br>取值约束：应为整数。 |

**返回值：**

| 类型 | 说明 |
| --- | --- |
| Long | 指定索引处的元素。 |


## at

```TypeScript
export function at(self: FixedArray<float>, index: int): Float
```

接受一个整数值并返回该索引处的元素，允许传入正整数和负整数。 负整数表示从数组最后一个元素开始倒数。

**起始版本：** 26.0.0

**ArkTS模式：** ArkTS-Sta起始版本为26.0.0。

**模型约束：** 此接口仅可在Stage模型下使用。

<!--Device-unnamed-export function at(self: FixedArray<float>, index: int): Float--><!--Device-unnamed-export function at(self: FixedArray<float>, index: int): Float-End-->

**系统能力：** SystemCapability.Utils.Lang

**参数：**

| 参数名 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| self | FixedArray&lt;float&gt; | 是 | 执行`at`操作的数组。 |
| index | int | 是 | 待返回数组元素的索引，从0开始计数。 <br>取值约束：应为整数。 |

**返回值：**

| 类型 | 说明 |
| --- | --- |
| Float | 指定索引处的元素。 |


## at

```TypeScript
export function at(self: FixedArray<double>, index: int): Double
```

接受一个整数值并返回该索引处的元素，允许传入正整数和负整数。 负整数表示从数组最后一个元素开始倒数。

**起始版本：** 26.0.0

**ArkTS模式：** ArkTS-Sta起始版本为26.0.0。

**模型约束：** 此接口仅可在Stage模型下使用。

<!--Device-unnamed-export function at(self: FixedArray<double>, index: int): Double--><!--Device-unnamed-export function at(self: FixedArray<double>, index: int): Double-End-->

**系统能力：** SystemCapability.Utils.Lang

**参数：**

| 参数名 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| self | FixedArray&lt;double&gt; | 是 | 执行`at`操作的数组。 |
| index | int | 是 | 待返回数组元素的索引，从0开始计数。 <br>取值约束：应为整数。 |

**返回值：**

| 类型 | 说明 |
| --- | --- |
| Double | 指定索引处的元素。 |


## at

```TypeScript
export function at(self: FixedArray<char>, index: int): Char
```

接受一个整数值并返回该索引处的元素，允许传入正整数和负整数。 负整数表示从数组最后一个元素开始倒数。

**起始版本：** 26.0.0

**ArkTS模式：** ArkTS-Sta起始版本为26.0.0。

**模型约束：** 此接口仅可在Stage模型下使用。

<!--Device-unnamed-export function at(self: FixedArray<char>, index: int): Char--><!--Device-unnamed-export function at(self: FixedArray<char>, index: int): Char-End-->

**系统能力：** SystemCapability.Utils.Lang

**参数：**

| 参数名 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| self | FixedArray&lt;char&gt; | 是 | 执行`at`操作的数组。 |
| index | int | 是 | 待返回数组元素的索引，从0开始计数。 <br>取值约束：应为整数。 |

**返回值：**

| 类型 | 说明 |
| --- | --- |
| Char | 指定索引处的元素。 |

