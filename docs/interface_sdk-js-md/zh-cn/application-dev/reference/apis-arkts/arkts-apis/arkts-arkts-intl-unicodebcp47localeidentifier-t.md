# UnicodeBCP47LocaleIdentifier

```TypeScript
export type UnicodeBCP47LocaleIdentifier = string
```

Unicode BCP 47区域设置标识符。

**起始版本：** 26.0.0

**ArkTS模式：** 仅支持ArkTS-Sta，ArkTS-Sta起始版本为26.0.0。

**模型约束：** 此接口仅可在Stage模型下使用。

**系统能力：** SystemCapability.Utils.Lang

**属性类型：** string
