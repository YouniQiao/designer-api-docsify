# concat

## 导入模块

```TypeScript
```

## concat

```TypeScript
export function concat(self: FixedArray<boolean>, fst: FixedArray<boolean>, ...more: FixedArray<FixedArray<boolean>>): 
    FixedArray<boolean>
```

将参数中提供的数组依次连接，合并两个或多个数组 为一个新数组。

**起始版本：** 26.0.0

**ArkTS模式：** 仅支持ArkTS-Sta，ArkTS-Sta起始版本为26.0.0。

**模型约束：** 此接口仅可在Stage模型下使用。

**系统能力：** SystemCapability.Utils.Lang

**参数：**

| 参数名 | 类型 | 必填 |
| --- | --- | --- |
| self | FixedArray & lt;boolean & gt; | 是 |
| fst | FixedArray & lt;boolean & gt; | 是 |
| more | FixedArray & lt;FixedArray & lt;boolean & gt; & gt; | 是 |

**返回值：**

| 类型 |
| --- |
| FixedArray & lt;boolean & gt; |


## concat

```TypeScript
export function concat(self: FixedArray<boolean>, ...items: FixedArray<ConcatArray<boolean>>): FixedArray<boolean>
```

根据当前数组实例与给定的数组实例创建新的数组。

**起始版本：** 26.0.0

**ArkTS模式：** 仅支持ArkTS-Sta，ArkTS-Sta起始版本为26.0.0。

**模型约束：** 此接口仅可在Stage模型下使用。

**系统能力：** SystemCapability.Utils.Lang

**参数：**

| 参数名 | 类型 | 必填 |
| --- | --- | --- |
| self | FixedArray & lt;boolean & gt; | 是 |
| items | FixedArray & lt;ConcatArray & lt;boolean & gt; & gt; | 是 |

**返回值：**

| 类型 |
| --- |
| FixedArray & lt;boolean & gt; |


## concat

```TypeScript
export function concat(self: FixedArray<byte>, fst: FixedArray<byte>, ...more: FixedArray<FixedArray<byte>>): 
    FixedArray<byte>
```

将参数中提供的数组依次连接，合并两个或多个数组 为一个新数组。

**起始版本：** 26.0.0

**ArkTS模式：** 仅支持ArkTS-Sta，ArkTS-Sta起始版本为26.0.0。

**模型约束：** 此接口仅可在Stage模型下使用。

**系统能力：** SystemCapability.Utils.Lang

**参数：**

| 参数名 | 类型 | 必填 |
| --- | --- | --- |
| self | FixedArray & lt;byte & gt; | 是 |
| fst | FixedArray & lt;byte & gt; | 是 |
| more | FixedArray & lt;FixedArray & lt;byte & gt; & gt; | 是 |

**返回值：**

| 类型 |
| --- |
| FixedArray & lt;byte & gt; |


## concat

```TypeScript
export function concat(self: FixedArray<byte>, ...items: FixedArray<ConcatArray<byte>>): FixedArray<byte>
```

根据当前数组实例与给定的数组实例创建新的数组。

**起始版本：** 26.0.0

**ArkTS模式：** 仅支持ArkTS-Sta，ArkTS-Sta起始版本为26.0.0。

**模型约束：** 此接口仅可在Stage模型下使用。

**系统能力：** SystemCapability.Utils.Lang

**参数：**

| 参数名 | 类型 | 必填 |
| --- | --- | --- |
| self | FixedArray & lt;byte & gt; | 是 |
| items | FixedArray & lt;ConcatArray & lt;byte & gt; & gt; | 是 |

**返回值：**

| 类型 |
| --- |
| FixedArray & lt;byte & gt; |


## concat

```TypeScript
export function concat(self: FixedArray<short>, fst: FixedArray<short>, ...more: FixedArray<FixedArray<short>>): 
    FixedArray<short>
```

将参数中提供的数组依次连接，合并两个或多个数组 为一个新数组。

**起始版本：** 26.0.0

**ArkTS模式：** 仅支持ArkTS-Sta，ArkTS-Sta起始版本为26.0.0。

**模型约束：** 此接口仅可在Stage模型下使用。

**系统能力：** SystemCapability.Utils.Lang

**参数：**

| 参数名 | 类型 | 必填 |
| --- | --- | --- |
| self | FixedArray & lt;short & gt; | 是 |
| fst | FixedArray & lt;short & gt; | 是 |
| more | FixedArray & lt;FixedArray & lt;short & gt; & gt; | 是 |

**返回值：**

| 类型 |
| --- |
| FixedArray & lt;short & gt; |


## concat

```TypeScript
export function concat(self: FixedArray<short>, ...items: FixedArray<ConcatArray<short>>): FixedArray<short>
```

根据当前数组实例与给定的数组实例创建新的数组。

**起始版本：** 26.0.0

**ArkTS模式：** 仅支持ArkTS-Sta，ArkTS-Sta起始版本为26.0.0。

**模型约束：** 此接口仅可在Stage模型下使用。

**系统能力：** SystemCapability.Utils.Lang

**参数：**

| 参数名 | 类型 | 必填 |
| --- | --- | --- |
| self | FixedArray & lt;short & gt; | 是 |
| items | FixedArray & lt;ConcatArray & lt;short & gt; & gt; | 是 |

**返回值：**

| 类型 |
| --- |
| FixedArray & lt;short & gt; |


## concat

```TypeScript
export function concat(self: FixedArray<int>, fst: FixedArray<int>, ...more: FixedArray<FixedArray<int>>): 
    FixedArray<int>
```

将参数中提供的数组依次连接，合并两个或多个数组 为一个新数组。

**起始版本：** 26.0.0

**ArkTS模式：** 仅支持ArkTS-Sta，ArkTS-Sta起始版本为26.0.0。

**模型约束：** 此接口仅可在Stage模型下使用。

**系统能力：** SystemCapability.Utils.Lang

**参数：**

| 参数名 | 类型 | 必填 |
| --- | --- | --- |
| self | FixedArray & lt;int & gt; | 是 |
| fst | FixedArray & lt;int & gt; | 是 |
| more | FixedArray & lt;FixedArray & lt;int & gt; & gt; | 是 |

**返回值：**

| 类型 |
| --- |
| FixedArray & lt;int & gt; |


## concat

```TypeScript
export function concat(self: FixedArray<int>, ...items: FixedArray<ConcatArray<int>>): FixedArray<int>
```

根据当前数组实例与给定的数组实例创建新的数组。

**起始版本：** 26.0.0

**ArkTS模式：** 仅支持ArkTS-Sta，ArkTS-Sta起始版本为26.0.0。

**模型约束：** 此接口仅可在Stage模型下使用。

**系统能力：** SystemCapability.Utils.Lang

**参数：**

| 参数名 | 类型 | 必填 |
| --- | --- | --- |
| self | FixedArray & lt;int & gt; | 是 |
| items | FixedArray & lt;ConcatArray & lt;int & gt; & gt; | 是 |

**返回值：**

| 类型 |
| --- |
| FixedArray & lt;int & gt; |


## concat

```TypeScript
export function concat(self: FixedArray<long>, fst: FixedArray<long>, ...more: FixedArray<FixedArray<long>>): 
    FixedArray<long>
```

将参数中提供的数组依次连接，合并两个或多个数组 为一个新数组。

**起始版本：** 26.0.0

**ArkTS模式：** 仅支持ArkTS-Sta，ArkTS-Sta起始版本为26.0.0。

**模型约束：** 此接口仅可在Stage模型下使用。

**系统能力：** SystemCapability.Utils.Lang

**参数：**

| 参数名 | 类型 | 必填 |
| --- | --- | --- |
| self | FixedArray & lt;long & gt; | 是 |
| fst | FixedArray & lt;long & gt; | 是 |
| more | FixedArray & lt;FixedArray & lt;long & gt; & gt; | 是 |

**返回值：**

| 类型 |
| --- |
| FixedArray & lt;long & gt; |


## concat

```TypeScript
export function concat(self: FixedArray<long>, ...items: FixedArray<ConcatArray<long>>): FixedArray<long>
```

根据当前数组实例与给定的数组实例创建新的数组。

**起始版本：** 26.0.0

**ArkTS模式：** 仅支持ArkTS-Sta，ArkTS-Sta起始版本为26.0.0。

**模型约束：** 此接口仅可在Stage模型下使用。

**系统能力：** SystemCapability.Utils.Lang

**参数：**

| 参数名 | 类型 | 必填 |
| --- | --- | --- |
| self | FixedArray & lt;long & gt; | 是 |
| items | FixedArray & lt;ConcatArray & lt;long & gt; & gt; | 是 |

**返回值：**

| 类型 |
| --- |
| FixedArray & lt;long & gt; |


## concat

```TypeScript
export function concat(self: FixedArray<float>, fst: FixedArray<float>, ...more: FixedArray<FixedArray<float>>): 
    FixedArray<float>
```

将参数中提供的数组依次连接，合并两个或多个数组 为一个新数组。

**起始版本：** 26.0.0

**ArkTS模式：** 仅支持ArkTS-Sta，ArkTS-Sta起始版本为26.0.0。

**模型约束：** 此接口仅可在Stage模型下使用。

**系统能力：** SystemCapability.Utils.Lang

**参数：**

| 参数名 | 类型 | 必填 |
| --- | --- | --- |
| self | FixedArray & lt;float & gt; | 是 |
| fst | FixedArray & lt;float & gt; | 是 |
| more | FixedArray & lt;FixedArray & lt;float & gt; & gt; | 是 |

**返回值：**

| 类型 |
| --- |
| FixedArray & lt;float & gt; |


## concat

```TypeScript
export function concat(self: FixedArray<float>, ...items: FixedArray<ConcatArray<float>>): FixedArray<float>
```

根据当前数组实例与给定的数组实例创建新的数组。

**起始版本：** 26.0.0

**ArkTS模式：** 仅支持ArkTS-Sta，ArkTS-Sta起始版本为26.0.0。

**模型约束：** 此接口仅可在Stage模型下使用。

**系统能力：** SystemCapability.Utils.Lang

**参数：**

| 参数名 | 类型 | 必填 |
| --- | --- | --- |
| self | FixedArray & lt;float & gt; | 是 |
| items | FixedArray & lt;ConcatArray & lt;float & gt; & gt; | 是 |

**返回值：**

| 类型 |
| --- |
| FixedArray & lt;float & gt; |


## concat

```TypeScript
export function concat(self: FixedArray<double>, fst: FixedArray<double>, ...more: FixedArray<FixedArray<double>>): 
    FixedArray<double>
```

将参数中提供的数组依次连接，合并两个或多个数组 为一个新数组。

**起始版本：** 26.0.0

**ArkTS模式：** 仅支持ArkTS-Sta，ArkTS-Sta起始版本为26.0.0。

**模型约束：** 此接口仅可在Stage模型下使用。

**系统能力：** SystemCapability.Utils.Lang

**参数：**

| 参数名 | 类型 | 必填 |
| --- | --- | --- |
| self | FixedArray & lt;double & gt; | 是 |
| fst | FixedArray & lt;double & gt; | 是 |
| more | FixedArray & lt;FixedArray & lt;double & gt; & gt; | 是 |

**返回值：**

| 类型 |
| --- |
| FixedArray & lt;double & gt; |


## concat

```TypeScript
export function concat(self: FixedArray<double>, ...items: FixedArray<ConcatArray<double>>): FixedArray<double>
```

根据当前数组实例与给定的数组实例创建新的数组。

**起始版本：** 26.0.0

**ArkTS模式：** 仅支持ArkTS-Sta，ArkTS-Sta起始版本为26.0.0。

**模型约束：** 此接口仅可在Stage模型下使用。

**系统能力：** SystemCapability.Utils.Lang

**参数：**

| 参数名 | 类型 | 必填 |
| --- | --- | --- |
| self | FixedArray & lt;double & gt; | 是 |
| items | FixedArray & lt;ConcatArray & lt;double & gt; & gt; | 是 |

**返回值：**

| 类型 |
| --- |
| FixedArray & lt;double & gt; |


## concat

```TypeScript
export function concat(self: FixedArray<char>, fst: FixedArray<char>, ...more: FixedArray<FixedArray<char>>): 
    FixedArray<char>
```

将参数中提供的数组依次连接，合并两个或多个数组 为一个新数组。

**起始版本：** 26.0.0

**ArkTS模式：** 仅支持ArkTS-Sta，ArkTS-Sta起始版本为26.0.0。

**模型约束：** 此接口仅可在Stage模型下使用。

**系统能力：** SystemCapability.Utils.Lang

**参数：**

| 参数名 | 类型 | 必填 |
| --- | --- | --- |
| self | FixedArray & lt;char & gt; | 是 |
| fst | FixedArray & lt;char & gt; | 是 |
| more | FixedArray & lt;FixedArray & lt;char & gt; & gt; | 是 |

**返回值：**

| 类型 |
| --- |
| FixedArray & lt;char & gt; |


## concat

```TypeScript
export function concat(self: FixedArray<char>, ...items: FixedArray<ConcatArray<char>>): FixedArray<char>
```

根据当前数组实例与给定的数组实例创建新的数组。

**起始版本：** 26.0.0

**ArkTS模式：** 仅支持ArkTS-Sta，ArkTS-Sta起始版本为26.0.0。

**模型约束：** 此接口仅可在Stage模型下使用。

**系统能力：** SystemCapability.Utils.Lang

**参数：**

| 参数名 | 类型 | 必填 |
| --- | --- | --- |
| self | FixedArray & lt;char & gt; | 是 |
| items | FixedArray & lt;ConcatArray & lt;char & gt; & gt; | 是 |

**返回值：**

| 类型 |
| --- |
| FixedArray & lt;char & gt; |
