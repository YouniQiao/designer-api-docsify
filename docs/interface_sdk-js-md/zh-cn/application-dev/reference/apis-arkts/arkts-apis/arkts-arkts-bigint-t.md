# bigint

```TypeScript
export type bigint = BigInt
```

`bigint`是类型`BigInt`的别名。

**起始版本：** 26.0.0

**ArkTS模式：** 仅支持ArkTS-Sta，ArkTS-Sta起始版本为26.0.0。

**模型约束：** 此接口仅可在Stage模型下使用。

**系统能力：** SystemCapability.Utils.Lang

**属性类型：** [BigInt](arkts-arkts-bigint-c.md)
