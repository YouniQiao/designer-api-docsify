# RelativeTimeFormatLocaleMatcher

```TypeScript
export type RelativeTimeFormatLocaleMatcher = 'lookup' | 'best fit'
```

相对时间格式化的区域匹配算法。

**起始版本：** 26.0.0

**ArkTS模式：** ArkTS-Sta起始版本为26.0.0。

**模型约束：** 此接口仅可在Stage模型下使用。

<!--Device-Intl-export type RelativeTimeFormatLocaleMatcher = 'lookup' | 'best fit'--><!--Device-Intl-export type RelativeTimeFormatLocaleMatcher = 'lookup' | 'best fit'-End-->

**系统能力：** SystemCapability.Utils.Lang

| 类型 | 说明 |
| --- | --- |
| 'lookup' |  |
| 'best fit' |  |

