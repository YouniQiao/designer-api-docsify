# BuiltinArrayAlgorithms

## 导入模块

```TypeScript
```

## 汇总

### 函数

| 名称 | 说明 |
| --- | --- |
| [copyOf](arkts-arkts-builtinarrayalgorithms-copyof-f.md) | 创建数组的浅拷贝。 |
| [copyOf](arkts-arkts-builtinarrayalgorithms-copyof-f.md) | 创建数组的浅拷贝。 |
| [copyOf](arkts-arkts-builtinarrayalgorithms-copyof-f.md) | 创建数组的浅拷贝。 |
| [copyOf](arkts-arkts-builtinarrayalgorithms-copyof-f.md) | 创建数组的浅拷贝。 |
| [copyOf](arkts-arkts-builtinarrayalgorithms-copyof-f.md) | 创建数组的浅拷贝。 |
| [copyOf](arkts-arkts-builtinarrayalgorithms-copyof-f.md) | 创建数组的浅拷贝。 |
| [copyOf](arkts-arkts-builtinarrayalgorithms-copyof-f.md) | 创建数组的浅拷贝。 |
| [copyOf](arkts-arkts-builtinarrayalgorithms-copyof-f.md) | 创建数组的浅拷贝。 |
| [copyOf](arkts-arkts-builtinarrayalgorithms-copyof-f.md) | 创建数组的浅拷贝。 |
| [copyTo](arkts-arkts-builtinarrayalgorithms-copyto-f.md) | 按传入的索引将src数组复制到dst中。 dst必须有足够的空间，否则可能发生越界。 |
| [copyTo](arkts-arkts-builtinarrayalgorithms-copyto-f.md) | 按传入的索引将src数组复制到dst中。 dst必须有足够的空间，否则可能发生越界。 |
| [copyTo](arkts-arkts-builtinarrayalgorithms-copyto-f.md) | 按传入的索引将src数组复制到dst中。 dst必须有足够的空间，否则可能发生越界。 |
| [copyTo](arkts-arkts-builtinarrayalgorithms-copyto-f.md) | 按传入的索引将src数组复制到dst中。 dst必须有足够的空间，否则可能发生越界。 |
| [copyTo](arkts-arkts-builtinarrayalgorithms-copyto-f.md) | 按传入的索引将src数组复制到dst中。 dst必须有足够的空间，否则可能发生越界。 |
| [copyTo](arkts-arkts-builtinarrayalgorithms-copyto-f.md) | 按传入的索引将src数组复制到dst中。 dst必须有足够的空间，否则可能发生越界。 |
| [copyTo](arkts-arkts-builtinarrayalgorithms-copyto-f.md) | 按传入的索引将src数组复制到dst中。 dst必须有足够的空间，否则可能发生越界。 |
| [copyTo](arkts-arkts-builtinarrayalgorithms-copyto-f.md) | 按传入的索引将src数组复制到dst中。 dst必须有足够的空间，否则可能发生越界。 |
| [copyTo](arkts-arkts-builtinarrayalgorithms-copyto-f.md) | 按传入的索引将src数组复制到dst中。 dst必须有足够的空间，否则可能发生越界。 |
| [lowerBoundSearch](arkts-arkts-builtinarrayalgorithms-lowerboundsearch-f.md) | 尝试在已排序的arr中查找键的下界。 调用该函数前必须先对数组排序。 下界是第一个使(element &lt; key)不成立的元素的索引。 若不存在这样的元素，则下界为endIndex。 |
| [lowerBoundSearch](arkts-arkts-builtinarrayalgorithms-lowerboundsearch-f.md) | 尝试在已排序的arr中查找键的下界。 调用该函数前必须先对数组排序。 下界是第一个使(element &lt; key)不成立的元素的索引。 若不存在这样的元素，则下界为arr.length。 |
| [lowerBoundSearch](arkts-arkts-builtinarrayalgorithms-lowerboundsearch-f.md) | 尝试在已排序的arr中查找键的下界。 调用该函数前必须先对数组排序。 下界是第一个使(element &lt; key)不成立的元素的索引。 若不存在这样的元素，则下界为endIndex。 |
| [lowerBoundSearch](arkts-arkts-builtinarrayalgorithms-lowerboundsearch-f.md) | 尝试在已排序的arr中查找键的下界。 调用该函数前必须先对数组排序。 下界是第一个使(element &lt; key)不成立的元素的索引。 若不存在这样的元素，则下界为arr.length。 |
| [lowerBoundSearch](arkts-arkts-builtinarrayalgorithms-lowerboundsearch-f.md) | 尝试在已排序的arr中查找键的下界。 调用该函数前必须先对数组排序。 下界是第一个使(element &lt; key)不成立的元素的索引。 若不存在这样的元素，则下界为endIndex。 |
| [lowerBoundSearch](arkts-arkts-builtinarrayalgorithms-lowerboundsearch-f.md) | 尝试在已排序的arr中查找键的下界。 调用该函数前必须先对数组排序。 下界是第一个使(element &lt; key)不成立的元素的索引。 若不存在这样的元素，则下界为arr.length。 |
| [lowerBoundSearch](arkts-arkts-builtinarrayalgorithms-lowerboundsearch-f.md) | 尝试在已排序的arr中查找键的下界。 调用该函数前必须先对数组排序。 下界是第一个使(element &lt; key)不成立的元素的索引。 若不存在这样的元素，则下界为endIndex。 |
| [lowerBoundSearch](arkts-arkts-builtinarrayalgorithms-lowerboundsearch-f.md) | 尝试在已排序的arr中查找键的下界。 调用该函数前必须先对数组排序。 下界是第一个使(element &lt; key)不成立的元素的索引。 若不存在这样的元素，则下界为arr.length。 |
| [lowerBoundSearch](arkts-arkts-builtinarrayalgorithms-lowerboundsearch-f.md) | 尝试在已排序的arr中查找键的下界。 调用该函数前必须先对数组排序。 下界是第一个使(element &lt; key)不成立的元素的索引。 若不存在这样的元素，则下界为endIndex。 |
| [lowerBoundSearch](arkts-arkts-builtinarrayalgorithms-lowerboundsearch-f.md) | 尝试在已排序的arr中查找键的下界。 调用该函数前必须先对数组排序。 下界是第一个使(element &lt; key)不成立的元素的索引。 若不存在这样的元素，则下界为arr.length。 |
| [lowerBoundSearch](arkts-arkts-builtinarrayalgorithms-lowerboundsearch-f.md) | 尝试在已排序的arr中查找键的下界。 调用该函数前必须先对数组排序。 下界是第一个使(element &lt; key)不成立的元素的索引。 若不存在这样的元素，则下界为endIndex。 |
| [lowerBoundSearch](arkts-arkts-builtinarrayalgorithms-lowerboundsearch-f.md) | 尝试在已排序的arr中查找键的下界。 调用该函数前必须先对数组排序。 下界是第一个使(element &lt; key)不成立的元素的索引。 若不存在这样的元素，则下界为arr.length。 |
| [lowerBoundSearch](arkts-arkts-builtinarrayalgorithms-lowerboundsearch-f.md) | 尝试在已排序的arr中查找键的下界。 调用该函数前必须先对数组排序。 下界是第一个使(element &lt; key)不成立的元素的索引。 若不存在这样的元素，则下界为endIndex。 |
| [lowerBoundSearch](arkts-arkts-builtinarrayalgorithms-lowerboundsearch-f.md) | 尝试在已排序的arr中查找键的下界。 调用该函数前必须先对数组排序。 下界是第一个使(element &lt; key)不成立的元素的索引。 若不存在这样的元素，则下界为arr.length。 |
| [lowerBoundSearch](arkts-arkts-builtinarrayalgorithms-lowerboundsearch-f.md) | 尝试在已排序的arr中查找键的下界。 调用该函数前必须先对数组排序。 下界是第一个使(element &lt; key)不成立的元素的索引。 若不存在这样的元素，则下界为endIndex。 |
| [lowerBoundSearch](arkts-arkts-builtinarrayalgorithms-lowerboundsearch-f.md) | 尝试在已排序的arr中查找键的下界。 调用该函数前必须先对数组排序。 下界是第一个使(element &lt; key)不成立的元素的索引。 若不存在这样的元素，则下界为arr.length。 |
| [upperBoundSearch](arkts-arkts-builtinarrayalgorithms-upperboundsearch-f.md) | 尝试在已排序的arr中查找键的上界。 调用该函数前必须先对数组排序。 上界是第一个使(key &lt; element)成立的元素的索引。 若不存在这样的元素，则上界为endIndex。 |
| [upperBoundSearch](arkts-arkts-builtinarrayalgorithms-upperboundsearch-f.md) | 尝试在已排序的arr中查找键的上界。 调用该函数前必须先对数组排序。 上界是第一个使(key &lt; element)成立的元素的索引。 若不存在这样的元素，则上界为endIndex。 |
| [upperBoundSearch](arkts-arkts-builtinarrayalgorithms-upperboundsearch-f.md) | 尝试在已排序的arr中查找键的上界。 调用该函数前必须先对数组排序。 上界是第一个使(key &lt; element)成立的元素的索引。 若不存在这样的元素，则上界为endIndex。 |
| [upperBoundSearch](arkts-arkts-builtinarrayalgorithms-upperboundsearch-f.md) | 尝试在已排序的arr中查找键的上界。 调用该函数前必须先对数组排序。 上界是第一个使(key &lt; element)成立的元素的索引。 若不存在这样的元素，则上界为endIndex。 |
| [upperBoundSearch](arkts-arkts-builtinarrayalgorithms-upperboundsearch-f.md) | 尝试在已排序的arr中查找键的上界。 调用该函数前必须先对数组排序。 上界是第一个使(key &lt; element)成立的元素的索引。 若不存在这样的元素，则上界为endIndex。 |
| [upperBoundSearch](arkts-arkts-builtinarrayalgorithms-upperboundsearch-f.md) | 尝试在已排序的arr中查找键的上界。 调用该函数前必须先对数组排序。 上界是第一个使(key &lt; element)成立的元素的索引。 若不存在这样的元素，则上界为endIndex。 |
| [upperBoundSearch](arkts-arkts-builtinarrayalgorithms-upperboundsearch-f.md) | 尝试在已排序的arr中查找键的上界。 调用该函数前必须先对数组排序。 上界是第一个使(key &lt; element)成立的元素的索引。 若不存在这样的元素，则上界为endIndex。 |
| [upperBoundSearch](arkts-arkts-builtinarrayalgorithms-upperboundsearch-f.md) | 尝试在已排序的arr中查找键的上界。 调用该函数前必须先对数组排序。 上界是第一个使(key &lt; element)成立的元素的索引。 若不存在这样的元素，则上界为endIndex。 |
| [upperBoundSearch](arkts-arkts-builtinarrayalgorithms-upperboundsearch-f.md) | 尝试在已排序的arr中查找键的上界。 调用该函数前必须先对数组排序。 上界是第一个使(key &lt; element)成立的元素的索引。 若不存在这样的元素，则上界为endIndex。 |
| [upperBoundSearch](arkts-arkts-builtinarrayalgorithms-upperboundsearch-f.md) | 尝试在已排序的arr中查找键的上界。 调用该函数前必须先对数组排序。 上界是第一个使(key &lt; element)成立的元素的索引。 若不存在这样的元素，则上界为endIndex。 |
| [upperBoundSearch](arkts-arkts-builtinarrayalgorithms-upperboundsearch-f.md) | 尝试在已排序的arr中查找键的上界。 调用该函数前必须先对数组排序。 上界是第一个使(key &lt; element)成立的元素的索引。 若不存在这样的元素，则上界为endIndex。 |
| [upperBoundSearch](arkts-arkts-builtinarrayalgorithms-upperboundsearch-f.md) | 尝试在已排序的arr中查找键的上界。 调用该函数前必须先对数组排序。 上界是第一个使(key &lt; element)成立的元素的索引。 若不存在这样的元素，则上界为endIndex。 |
| [upperBoundSearch](arkts-arkts-builtinarrayalgorithms-upperboundsearch-f.md) | 尝试在已排序的arr中查找键的上界。 调用该函数前必须先对数组排序。 上界是第一个使(key &lt; element)成立的元素的索引。 若不存在这样的元素，则上界为endIndex。 |
| [upperBoundSearch](arkts-arkts-builtinarrayalgorithms-upperboundsearch-f.md) | 尝试在已排序的arr中查找键的上界。 调用该函数前必须先对数组排序。 上界是第一个使(key &lt; element)成立的元素的索引。 若不存在这样的元素，则上界为endIndex。 |
| [upperBoundSearch](arkts-arkts-builtinarrayalgorithms-upperboundsearch-f.md) | 尝试在已排序的arr中查找键的上界。 调用该函数前必须先对数组排序。 上界是第一个使(key &lt; element)成立的元素的索引。 若不存在这样的元素，则上界为endIndex。 |
| [upperBoundSearch](arkts-arkts-builtinarrayalgorithms-upperboundsearch-f.md) | 尝试在已排序的arr中查找键的上界。 调用该函数前必须先对数组排序。 上界是第一个使(key &lt; element)成立的元素的索引。 若不存在这样的元素，则上界为endIndex。 |

### 常量

| 名称 | 说明 |
| --- | --- |
| [KEY_NOT_FOUND](arkts-arkts-builtinarrayalgorithms-con.md#key_not_found) | 表示未找到键的常量。 |

