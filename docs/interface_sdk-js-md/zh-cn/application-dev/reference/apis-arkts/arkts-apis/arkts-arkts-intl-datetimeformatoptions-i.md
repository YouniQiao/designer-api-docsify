# DateTimeFormatOptions

日期时间格式化选项。

**起始版本：** 26.0.0

**ArkTS模式：** 仅支持ArkTS-Sta，ArkTS-Sta起始版本为26.0.0。

**系统能力：** SystemCapability.Utils.Lang

## 导入模块

```TypeScript
```

## calendar

```TypeScript
calendar?: string
```

日历选项。

**类型：** string

**起始版本：** 26.0.0

**ArkTS模式：** 仅支持ArkTS-Sta，ArkTS-Sta起始版本为26.0.0。

**模型约束：** 此接口仅可在Stage模型下使用。

**系统能力：** SystemCapability.Utils.Lang

## dateStyle

```TypeScript
dateStyle?: DateStyle
```

日期样式选项。

**类型：** [DateStyle](arkts-arkts-intl-datestyle-t.md)

**起始版本：** 26.0.0

**ArkTS模式：** 仅支持ArkTS-Sta，ArkTS-Sta起始版本为26.0.0。

**模型约束：** 此接口仅可在Stage模型下使用。

**系统能力：** SystemCapability.Utils.Lang

## day

```TypeScript
day?: 'numeric' | '2-digit'
```

日选项。

**类型：** 'numeric' \| '2-digit'

**起始版本：** 26.0.0

**ArkTS模式：** 仅支持ArkTS-Sta，ArkTS-Sta起始版本为26.0.0。

**模型约束：** 此接口仅可在Stage模型下使用。

**系统能力：** SystemCapability.Utils.Lang

## dayPeriod

```TypeScript
dayPeriod?: 'narrow' | 'short' | 'long'
```

时段选项。

**类型：** 'narrow' \| 'short' \| 'long'

**起始版本：** 26.0.0

**ArkTS模式：** 仅支持ArkTS-Sta，ArkTS-Sta起始版本为26.0.0。

**模型约束：** 此接口仅可在Stage模型下使用。

**系统能力：** SystemCapability.Utils.Lang

## era

```TypeScript
era?: 'long' | 'short' | 'narrow'
```

纪元选项。

**类型：** 'long' \| 'short' \| 'narrow'

**起始版本：** 26.0.0

**ArkTS模式：** 仅支持ArkTS-Sta，ArkTS-Sta起始版本为26.0.0。

**模型约束：** 此接口仅可在Stage模型下使用。

**系统能力：** SystemCapability.Utils.Lang

## formatMatcher

```TypeScript
formatMatcher?: 'basic' | 'best fit'
```

格式匹配算法选项。

**类型：** 'basic' \| 'best fit'

**起始版本：** 26.0.0

**ArkTS模式：** 仅支持ArkTS-Sta，ArkTS-Sta起始版本为26.0.0。

**模型约束：** 此接口仅可在Stage模型下使用。

**系统能力：** SystemCapability.Utils.Lang

## fractionalSecondDigits

```TypeScript
fractionalSecondDigits?: double
```

秒的小数位数选项。 取值约束：应为整数。

**类型：** double

**起始版本：** 26.0.0

**ArkTS模式：** 仅支持ArkTS-Sta，ArkTS-Sta起始版本为26.0.0。

**模型约束：** 此接口仅可在Stage模型下使用。

**系统能力：** SystemCapability.Utils.Lang

## hour

```TypeScript
hour?: 'numeric' | '2-digit'
```

小时选项。

**类型：** 'numeric' \| '2-digit'

**起始版本：** 26.0.0

**ArkTS模式：** 仅支持ArkTS-Sta，ArkTS-Sta起始版本为26.0.0。

**模型约束：** 此接口仅可在Stage模型下使用。

**系统能力：** SystemCapability.Utils.Lang

## hour12

```TypeScript
hour12?: boolean
```

12小时制选项。

**类型：** boolean

**起始版本：** 26.0.0

**ArkTS模式：** 仅支持ArkTS-Sta，ArkTS-Sta起始版本为26.0.0。

**模型约束：** 此接口仅可在Stage模型下使用。

**系统能力：** SystemCapability.Utils.Lang

## hourCycle

```TypeScript
hourCycle?: 'h11' | 'h12' | 'h23' | 'h24'
```

小时制选项。

**类型：** 'h11' \| 'h12' \| 'h23' \| 'h24'

**起始版本：** 26.0.0

**ArkTS模式：** 仅支持ArkTS-Sta，ArkTS-Sta起始版本为26.0.0。

**模型约束：** 此接口仅可在Stage模型下使用。

**系统能力：** SystemCapability.Utils.Lang

## localeMatcher

```TypeScript
localeMatcher?: 'lookup' | 'best fit'
```

区域匹配算法选项。

**类型：** 'lookup' \| 'best fit'

**起始版本：** 26.0.0

**ArkTS模式：** 仅支持ArkTS-Sta，ArkTS-Sta起始版本为26.0.0。

**模型约束：** 此接口仅可在Stage模型下使用。

**系统能力：** SystemCapability.Utils.Lang

## minute

```TypeScript
minute?: 'numeric' | '2-digit'
```

分钟选项。

**类型：** 'numeric' \| '2-digit'

**起始版本：** 26.0.0

**ArkTS模式：** 仅支持ArkTS-Sta，ArkTS-Sta起始版本为26.0.0。

**模型约束：** 此接口仅可在Stage模型下使用。

**系统能力：** SystemCapability.Utils.Lang

## month

```TypeScript
month?: 'numeric' | '2-digit' | 'long' | 'short' | 'narrow'
```

月份选项。

**类型：** 'numeric' \| '2-digit' \| 'long' \| 'short' \| 'narrow'

**起始版本：** 26.0.0

**ArkTS模式：** 仅支持ArkTS-Sta，ArkTS-Sta起始版本为26.0.0。

**模型约束：** 此接口仅可在Stage模型下使用。

**系统能力：** SystemCapability.Utils.Lang

## numberingSystem

```TypeScript
numberingSystem?: string
```

记数系统选项。

**类型：** string

**起始版本：** 26.0.0

**ArkTS模式：** 仅支持ArkTS-Sta，ArkTS-Sta起始版本为26.0.0。

**模型约束：** 此接口仅可在Stage模型下使用。

**系统能力：** SystemCapability.Utils.Lang

## second

```TypeScript
second?: 'numeric' | '2-digit'
```

秒选项。

**类型：** 'numeric' \| '2-digit'

**起始版本：** 26.0.0

**ArkTS模式：** 仅支持ArkTS-Sta，ArkTS-Sta起始版本为26.0.0。

**模型约束：** 此接口仅可在Stage模型下使用。

**系统能力：** SystemCapability.Utils.Lang

## timeStyle

```TypeScript
timeStyle?: TimeStyle
```

时间样式选项。

**类型：** [TimeStyle](arkts-arkts-intl-timestyle-t.md)

**起始版本：** 26.0.0

**ArkTS模式：** 仅支持ArkTS-Sta，ArkTS-Sta起始版本为26.0.0。

**模型约束：** 此接口仅可在Stage模型下使用。

**系统能力：** SystemCapability.Utils.Lang

## timeZone

```TypeScript
timeZone?: string
```

时区选项。

**类型：** string

**起始版本：** 26.0.0

**ArkTS模式：** 仅支持ArkTS-Sta，ArkTS-Sta起始版本为26.0.0。

**模型约束：** 此接口仅可在Stage模型下使用。

**系统能力：** SystemCapability.Utils.Lang

## timeZoneName

```TypeScript
timeZoneName?: TimeZoneName
```

时区名称选项。

**类型：** [TimeZoneName](arkts-arkts-intl-timezonename-t.md)

**起始版本：** 26.0.0

**ArkTS模式：** 仅支持ArkTS-Sta，ArkTS-Sta起始版本为26.0.0。

**模型约束：** 此接口仅可在Stage模型下使用。

**系统能力：** SystemCapability.Utils.Lang

## weekday

```TypeScript
weekday?: 'long' | 'short' | 'narrow'
```

星期选项。

**类型：** 'long' \| 'short' \| 'narrow'

**起始版本：** 26.0.0

**ArkTS模式：** 仅支持ArkTS-Sta，ArkTS-Sta起始版本为26.0.0。

**模型约束：** 此接口仅可在Stage模型下使用。

**系统能力：** SystemCapability.Utils.Lang

## year

```TypeScript
year?: 'numeric' | '2-digit'
```

年份选项。

**类型：** 'numeric' \| '2-digit'

**起始版本：** 26.0.0

**ArkTS模式：** 仅支持ArkTS-Sta，ArkTS-Sta起始版本为26.0.0。

**模型约束：** 此接口仅可在Stage模型下使用。

**系统能力：** SystemCapability.Utils.Lang
