# arr_sort_stable

## 导入模块

```TypeScript
```

## arr_sort_stable

```TypeScript
export function arr_sort_stable(arr: FixedArray<Byte>, startIndex: int, endIndex: int,
    comp: (lhs: byte, rhs: byte) => int): void
```

使用稳定排序算法对数组进行原地排序。

**起始版本：** 26.0.0

**ArkTS模式：** 仅支持ArkTS-Sta，ArkTS-Sta起始版本为26.0.0。

**模型约束：** 此接口仅可在Stage模型下使用。

**系统能力：** SystemCapability.Utils.Lang

**参数：**

| 参数名 | 类型 | 必填 |
| --- | --- | --- |
| arr | FixedArray & lt;Byte & gt; | 是 |
| startIndex | int | 是 |
| endIndex | int | 是 |
| comp | (lhs: byte, rhs: byte) = & gt; int | 是 |


## arr_sort_stable

```TypeScript
export function arr_sort_stable(arr: FixedArray<Short>, startIndex: int, endIndex: int,
    comp: (lhs: short, rhs: short) => int): void
```

使用稳定排序算法对数组进行原地排序。

**起始版本：** 26.0.0

**ArkTS模式：** 仅支持ArkTS-Sta，ArkTS-Sta起始版本为26.0.0。

**模型约束：** 此接口仅可在Stage模型下使用。

**系统能力：** SystemCapability.Utils.Lang

**参数：**

| 参数名 | 类型 | 必填 |
| --- | --- | --- |
| arr | FixedArray & lt;Short & gt; | 是 |
| startIndex | int | 是 |
| endIndex | int | 是 |
| comp | (lhs: short, rhs: short) = & gt; int | 是 |


## arr_sort_stable

```TypeScript
export function arr_sort_stable(arr: FixedArray<Int>, startIndex: int, endIndex: int,
    comp: (lhs: int, rhs: int) => int): void
```

使用稳定排序算法对数组进行原地排序。

**起始版本：** 26.0.0

**ArkTS模式：** 仅支持ArkTS-Sta，ArkTS-Sta起始版本为26.0.0。

**模型约束：** 此接口仅可在Stage模型下使用。

**系统能力：** SystemCapability.Utils.Lang

**参数：**

| 参数名 | 类型 | 必填 |
| --- | --- | --- |
| arr | FixedArray & lt;Int & gt; | 是 |
| startIndex | int | 是 |
| endIndex | int | 是 |
| comp | (lhs: int, rhs: int) = & gt; int | 是 |


## arr_sort_stable

```TypeScript
export function arr_sort_stable(arr: FixedArray<Long>, startIndex: int, endIndex: int,
    comp: (lhs: long, rhs: long) => int): void
```

使用稳定排序算法对数组进行原地排序。

**起始版本：** 26.0.0

**ArkTS模式：** 仅支持ArkTS-Sta，ArkTS-Sta起始版本为26.0.0。

**模型约束：** 此接口仅可在Stage模型下使用。

**系统能力：** SystemCapability.Utils.Lang

**参数：**

| 参数名 | 类型 | 必填 |
| --- | --- | --- |
| arr | FixedArray & lt;Long & gt; | 是 |
| startIndex | int | 是 |
| endIndex | int | 是 |
| comp | (lhs: long, rhs: long) = & gt; int | 是 |


## arr_sort_stable

```TypeScript
export function arr_sort_stable(arr: FixedArray<Float>, startIndex: int, endIndex: int,
    comp: (lhs: float, rhs: float) => int): void
```

使用稳定排序算法对数组进行原地排序。

**起始版本：** 26.0.0

**ArkTS模式：** 仅支持ArkTS-Sta，ArkTS-Sta起始版本为26.0.0。

**模型约束：** 此接口仅可在Stage模型下使用。

**系统能力：** SystemCapability.Utils.Lang

**参数：**

| 参数名 | 类型 | 必填 |
| --- | --- | --- |
| arr | FixedArray & lt;Float & gt; | 是 |
| startIndex | int | 是 |
| endIndex | int | 是 |
| comp | (lhs: float, rhs: float) = & gt; int | 是 |


## arr_sort_stable

```TypeScript
export function arr_sort_stable(arr: FixedArray<Double>, startIndex: int, endIndex: int,
    comp: (lhs: double, rhs: double) => int): void
```

使用稳定排序算法对数组进行原地排序。

**起始版本：** 26.0.0

**ArkTS模式：** 仅支持ArkTS-Sta，ArkTS-Sta起始版本为26.0.0。

**模型约束：** 此接口仅可在Stage模型下使用。

**系统能力：** SystemCapability.Utils.Lang

**参数：**

| 参数名 | 类型 | 必填 |
| --- | --- | --- |
| arr | FixedArray & lt;Double & gt; | 是 |
| startIndex | int | 是 |
| endIndex | int | 是 |
| comp | (lhs: double, rhs: double) = & gt; int | 是 |
