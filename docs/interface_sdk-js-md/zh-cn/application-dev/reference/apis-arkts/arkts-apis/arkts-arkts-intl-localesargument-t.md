# LocalesArgument

```TypeScript
export type LocalesArgument = UnicodeBCP47LocaleIdentifier | Locale | readonly (UnicodeBCP47LocaleIdentifier |
        Locale)[]
```

Locales argument type.

**起始版本：** 26.0.0

**ArkTS模式：** 仅支持ArkTS-Sta，起始版本为26.0.0。

**模型约束：** 此接口仅可在Stage模型下使用。

<!--Device-Intl-export type LocalesArgument = UnicodeBCP47LocaleIdentifier | Locale | readonly (UnicodeBCP47LocaleIdentifier |        Locale)[]--><!--Device-Intl-export type LocalesArgument = UnicodeBCP47LocaleIdentifier | Locale | readonly (UnicodeBCP47LocaleIdentifier |        Locale)[]-End-->

**系统能力：** SystemCapability.Utils.Lang

| 类型 | 说明 |
| --- | --- |
| UnicodeBCP47LocaleIdentifier |  |
| Locale |  |
| readonly (UnicodeBCP47LocaleIdentifier |  |
| Locale)[] |  |

